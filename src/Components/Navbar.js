import React from "react";
import Icon from "../img/airbnb_icon.png"

export default function Navbar(params) {
    return (
        <nav className="navbar">
            <img src = {Icon} className ="nav--icon"/>
        </nav>
    )
    
}